<?php
    use app\Model\{
        CustomerModel,
        ReportModel,
        ResearchRunModel,
        QaReviewModel,
        UserModel
    };
?>

<div class="card mb-4">
    <div class="card-body">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h5 class="card-title mb-0"><?= htmlspecialchars($customer->companyName) ?></h5>
            <a href="/customers" class="btn btn-sm btn-outline-secondary"> << Back </a>
        </div>

        <?php if (in_array($this->session->get('userRole'), [UserModel::ROLE_ADMIN, UserModel::ROLE_DEV])) { ?>
            <form method="POST" action="/customer/<?= $customer->id ?>/save">
                <div class="row g-3">
                    <div class="col-md-6">
                        <label class="form-label">Company name</label>
                        <input type="text" name="company_name" class="form-control" value="<?= htmlspecialchars($customer->companyName) ?>">
                    </div>
                    
                    <div class="col-md-3">
                        <label class="form-label">Country</label>
                        <input type="text" name="country_code" class="form-control" value="<?= htmlspecialchars($customer->countryCode) ?>" maxlength="2">
                    </div>
                    
                    <div class="col-md-3">
                        <label class="form-label">Region</label>
                        <input type="text" name="region" class="form-control" value="<?= htmlspecialchars($customer->region ?? '') ?>">
                    </div>
                    
                    <div class="col-md-6">
                        <label class="form-label">Industry</label>
                        <input type="text" name="industry" class="form-control" value="<?= htmlspecialchars($customer->industry) ?>">
                    </div>
                    
                    <div class="col-md-3">
                        <label class="form-label">Employee band</label>
                        <input type="text" name="employee_band" class="form-control" value="<?= htmlspecialchars($customer->employeeBand) ?>">
                    </div>
                    
                    <div class="col-md-3">
                        <label class="form-label">AI maturity</label>
                        <select name="ai_maturity" class="form-select">
                            <?php foreach ([CustomerModel::MATURITY_NONE, CustomerModel::MATURITY_EXPLORING, CustomerModel::MATURITY_PILOT, CustomerModel::MATURITY_PRODUCTION] as $maturity) { ?>
                                <option value="<?= $maturity ?>" <?= $customer->aiMaturity === $maturity ? 'selected' : '' ?>><?= ucfirst($maturity) ?></option>
                            <?php } ?>
                        </select>
                    </div>
                    
                    <div class="col-md-6">
                        <label class="form-label">Contact name</label>
                        <input type="text" name="primary_contact_name" class="form-control" value="<?= htmlspecialchars($customer->primaryContactName ?? '') ?>">
                    </div>
                    
                    <div class="col-md-6">
                        <label class="form-label">Contact email</label>
                        <input type="email" name="primary_contact_email" class="form-control" value="<?= htmlspecialchars($customer->primaryContactEmail ?? '') ?>">
                    </div>
                    
                    <div class="col-md-6">
                        <label class="form-label">Status</label>
                        <select name="status" class="form-select">
                            <?php foreach ([CustomerModel::STATUS_ACTIVE, CustomerModel::STATUS_PAUSED, CustomerModel::STATUS_ARCHIVED] as $status) { ?>
                                <option value="<?= $status ?>" <?= $customer->status === $status ? 'selected' : '' ?>><?= ucfirst($status) ?></option>
                            <?php } ?>
                        </select>
                    </div>
                    
                    <div class="col-12">
                        <label class="form-label">Notes</label>
                        <textarea name="notes" class="form-control" rows="3"><?= htmlspecialchars($customer->notes ?? '') ?></textarea>
                    </div>
                </div>
                
                <div class="mt-3">
                    <button type="submit" class="btn btn-primary">Save</button>
                </div>
            </form>
        <?php } else { ?>
            <table class="table table-sm mb-0">
                <tr><td class="text-muted">Company</td><td><?= htmlspecialchars($customer->companyName) ?></td></tr>
                <tr><td class="text-muted">Country</td><td><?= htmlspecialchars($customer->countryCode) ?></td></tr>
                <tr><td class="text-muted">Region</td><td><?= htmlspecialchars($customer->region ?? '—') ?></td></tr>
                <tr><td class="text-muted">Industry</td><td><?= htmlspecialchars($customer->industry) ?></td></tr>
                <tr><td class="text-muted">Employee band</td><td><?= htmlspecialchars($customer->employeeBand) ?></td></tr>
                <tr><td class="text-muted">AI maturity</td><td><?= ucfirst($customer->aiMaturity) ?></td></tr>
                <tr><td class="text-muted">Contact name</td><td><?= htmlspecialchars($customer->primaryContactName ?? '—') ?></td></tr>
                <tr><td class="text-muted">Contact email</td><td><?= htmlspecialchars($customer->primaryContactEmail ?? '—') ?></td></tr>
                <tr><td class="text-muted">Status</td><td><?= ucfirst($customer->status) ?></td></tr>
                <tr><td class="text-muted">Notes</td><td><?= htmlspecialchars($customer->notes ?? '—') ?></td></tr>
            </table>
        <?php } ?>
    </div>
</div>

<div class="row g-4 mb-4">
    <div class="col-md-6">
        <div class="card h-100">
            <div class="card-body">
                <h6 class="card-title mb-3">Latest run</h6>
                <?php if ($latestRun) { ?>
                    <table class="table table-sm mb-0">
                        <tr>
                            <td class="text-muted">Status</td>
                            <td>
                                <span class="badge bg-<?= match($latestRun['status']) {
                                    ResearchRunModel::STATUS_COMPLETED => 'success',
                                    ResearchRunModel::STATUS_FAILED    => 'danger',
                                    ResearchRunModel::STATUS_RUNNING   => 'primary',
                                    ResearchRunModel::STATUS_BLOCKED   => 'warning',
                                    default                            => 'secondary'
                                } ?>"><?= ucfirst($latestRun['status']) ?></span>
                            </td>
                        </tr>
                        <tr>
                            <td class="text-muted">Guardrail</td>
                            <td>
                                <span class="badge bg-<?= match($latestRun['guardrail_status'] ?? '') {
                                    ResearchRunModel::STATUS_PASS    => 'success',
                                    ResearchRunModel::STATUS_REVIEW  => 'warning',
                                    ResearchRunModel::STATUS_BLOCKED => 'danger',
                                    default                          => 'secondary'
                                } ?>"><?= ucfirst($latestRun['guardrail_status'] ?? '—') ?></span>
                            </td>
                        </tr>
                        <tr><td class="text-muted">Started</td><td><?= $latestRun['started_at'] ?? '—' ?></td></tr>
                    </table>
                <?php } else { ?>
                    <p class="text-muted">No runs yet.</p>
                <?php } ?>
            </div>
        </div>
    </div>

    <div class="col-md-6">
        <div class="card h-100">
            <div class="card-body">
                <h6 class="card-title mb-3">Latest QA status</h6>
                <?php if ($latestQa) { ?>
                    <table class="table table-sm mb-0">
                        <tr>
                            <td class="text-muted">Decision</td>
                            <td>
                                <span class="badge bg-<?= match($latestQa['decision_status']) {
                                    QaReviewModel::STATUS_APPROVED => 'success',
                                    QaReviewModel::STATUS_REJECTED => 'danger',
                                    QaReviewModel::STATUS_PENDING  => 'info',
                                    default                        => 'secondary'
                                } ?>"><?= ucfirst($latestQa['decision_status']) ?></span>
                            </td>
                        </tr>
                        <tr><td class="text-muted">Decided at</td><td><?= $latestQa['decided_at'] ?? '—' ?></td></tr>
                    </table>
                <?php } else { ?>
                    <p class="text-muted">No QA reviews yet.</p>
                <?php } ?>
            </div>
        </div>
    </div>
</div>

<div class="card">
    <div class="card-body">
        <h6 class="card-title mb-3">Related reports</h6>
        <?php if (empty($reports)) { ?>
            <p class="text-muted">No reports yet.</p>
        <?php } else { ?>
            <table class="table table-sm">
                <thead>
                    <tr>
                        <th> # </th>
                        <th> Scope key </th>
                        <th> Status </th>
                        <th> Last updated </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    <?php foreach ($reports as $report) { ?>
                        <tr>
                            <td><?= $report['id'] ?></td>
                            <td><code><?= htmlspecialchars($report['canonical_scope_key']) ?></code></td>
                            <td>
                                <span class="badge bg-<?= match($report['status']) {
                                    ReportModel::STATUS_APPROVED  => 'success',
                                    ReportModel::STATUS_REJECTED  => 'danger',
                                    ReportModel::STATUS_NEEDS_QA  => 'info',
                                    ReportModel::STATUS_DRAFT     => 'secondary',
                                    default                       => 'secondary'
                                } ?>"><?= ucfirst(str_replace('_', ' ', $report['status'])) ?></span>
                            </td>
                            <td><?= $report['updated_at'] ?></td>
                            <td><a href="/report/<?= $report['id'] ?>" class="btn btn-sm btn-outline-primary">View</a></td>
                        </tr>
                    <?php } ?>
                </tbody>
            </table>
        <?php } ?>
    </div>
</div>
