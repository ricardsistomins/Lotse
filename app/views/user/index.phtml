<div class="mb-4 d-flex justify-content-between align-items-center">
    <div>
        <h2 class="text-success"><?= $translate('Users') ?></h2>
        <span class="text-muted fs-5"><?= $translate('Manage user accounts and access.') ?></span>
    </div>
    
    <a href="/<?= $language ?>/user/create" class="btn btn-success rounded-3">
        <i class="bi bi-person-plus me-1"></i> <?= $translate('Add user') ?>
    </a>
</div>

<div class="card rounded-4">
    <div class="card-body">
        <table class="table table-hover mb-0">
            <thead>
                <tr>
                    <th><?= $translate('Name') ?></th>
                    <th><?= $translate('Email') ?></th>
                    <th><?= $translate('Role') ?></th>
                    <th><?= $translate('Status') ?></th>
                    <th><?= $translate('Last login') ?></th>
                    <th></th>
                </tr>
            </thead>
            
            <tbody>
                <?php foreach ($users as $user) { ?>
                    <tr>
                        <td><?= htmlspecialchars($user->name . ' ' . $user->surname) ?></td>
                        <td><?= htmlspecialchars($user->email) ?></td>
                        <td>
                            <span class="badge text-bg-secondary"><?= htmlspecialchars($user->role) ?></span>
                        </td>
                        <td>
                            <?php if ($user->isActive) { ?>
                                <span class="badge text-bg-success"><?= $translate('Active') ?></span>
                            <?php } else { ?>
                                <span class="badge text-bg-danger"><?= $translate('Inactive') ?></span>
                            <?php } ?>
                        </td>
                        <td><small><?= $user->lastLoginAt ?? '—' ?></small></td>
                        <td>
                            <a href="/<?= $language ?>/user/<?= $user->userId ?>/edit" class="btn btn-sm btn-outline-secondary rounded-3">
                                <i class="bi bi-pencil me-1"></i> <?= $translate('Edit') ?>
                            </a>
                        </td>
                    </tr>
                <?php } ?>
            </tbody>
        </table>
    </div>
</div>
