<div class="mb-4 d-flex justify-content-between align-items-center">
    <div>
        <h2 class="text-success"><?= $translate('Add user') ?></h2>
        <span class="text-muted fs-5"><?= $translate('Create a new user account.') ?></span>
    </div>
    
    <a href="/<?= $language ?>/users" class="btn btn-outline-secondary rounded-3">
        <i class="bi bi-arrow-left me-1"></i> <?= $translate('Back') ?>
    </a>
</div>

<div class="card rounded-4">
    <div class="card-body">
        <?php if (!empty($error)) { ?>
            <div class="alert alert-danger rounded-3"><?= $translate($error) ?></div>
        <?php } ?>

        <form method="POST" action="/<?= $language ?>/user/create">
            <div class="row g-3">
                <div class="col-md-6">
                    <label class="form-label"><?= $translate('Name') ?></label>
                    <input type="text" name="name" class="form-control rounded-3"
                           value="<?= htmlspecialchars($name ?? '') ?>" required>
                </div>
                
                <div class="col-md-6">
                    <label class="form-label"><?= $translate('Surname') ?></label>
                    <input type="text" name="surname" class="form-control rounded-3"
                           value="<?= htmlspecialchars($surname ?? '') ?>" required>
                </div>
                
                <div class="col-md-6">
                    <label class="form-label"><?= $translate('Email') ?></label>
                    <input type="email" name="email" class="form-control rounded-3"
                           value="<?= htmlspecialchars($email ?? '') ?>" required>
                </div>
                
                <div class="col-md-6">
                    <label class="form-label"><?= $translate('Role') ?></label>
                    
                    <select name="role" class="form-select rounded-3" required>
                        <option value="admin"  <?= ($role ?? '') === 'admin' ? 'selected' : '' ?>><?= $translate('Admin') ?></option>
                        <option value="dev"    <?= ($role ?? '') === 'dev' ? 'selected' : '' ?>><?= $translate('Developer') ?></option>
                        <option value="qa"     <?= ($role ?? '') === 'qa' ? 'selected' : '' ?>><?= $translate('QA') ?></option>
                    </select>
                </div>
                
                <div class="col-md-6">
                    <label class="form-label"><?= $translate('Password') ?></label>
                    <input type="password" name="password" class="form-control rounded-3"
                           minlength="8" required>
                </div>
                
                 <div class="col-md-6">
                    <label class="form-label"><?= $translate('Confirm password') ?></label>
                    <input type="password" name="password_confirm" class="form-control rounded-3" minlength="8" required>
                </div>
            </div>

            <div class="mt-4">
                <button type="submit" class="btn btn-success rounded-3">
                    <i class="bi bi-person-check me-1"></i> <?= $translate('Create user') ?>
                </button>
            </div>
        </form>
    </div>
</div>
