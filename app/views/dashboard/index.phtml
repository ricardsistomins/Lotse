<?php
    use app\Model\{
        ResearchRunModel,
        ReportModel,
        CustomerModel
    };

    $runId    = $this->request->getQuery('runId', 'int');
    $reportId = $this->request->getQuery('reportId', 'int');
?>

<div class="row g-3 mb-4">
    <div class="col-md-3">
        <div class="card text-center">
            <div class="card-body">
                <div class="fs-2 fw-bold"><?= $activeCustomers ?></div>
                <div class="text-muted small">Active customers</div>
            </div>
        </div>
    </div>
    
    <div class="col-md-3">
        <div class="card text-center">
            <div class="card-body">
                <div class="fs-2 fw-bold text-primary"><?= $runsInProgress ?></div>
                <div class="text-muted small">Runs in progress</div>
            </div>
        </div>
    </div>
    
    <div class="col-md-3">
        <div class="card text-center">
            <div class="card-body">
                <div class="fs-2 fw-bold text-warning"><?= $blockedRuns ?></div>
                <div class="text-muted small">Blocked runs</div>
            </div>
        </div>
    </div>
    
    <div class="col-md-3">
        <div class="card text-center">
            <div class="card-body">
                <div class="fs-2 fw-bold text-info"><?= $reportsAwaitingQa ?></div>
                <div class="text-muted small">Reports awaiting QA</div>
            </div>
        </div>
    </div>
    
    <div class="col-md-3">
        <div class="card text-center">
            <div class="card-body">
                <div class="fs-2 fw-bold text-success"><?= $approvedReports ?></div>
                <div class="text-muted small">Approved reports</div>
            </div>
        </div>
    </div>
    
    <div class="col-md-3">
        <div class="card text-center">
            <div class="card-body">
                <div class="fs-2 fw-bold <?= $providerFailures24h > 0 ? 'text-danger' : 'text-success' ?>"><?= $providerFailures24h ?></div>
                <div class="text-muted small">Provider failures (24h)</div>
            </div>
        </div>
    </div>
    
    <div class="col-md-3">
        <div class="card text-center">
            <div class="card-body">
                <div class="fs-2 fw-bold <?= $guardrailBlocks24h > 0 ? 'text-danger' : 'text-success' ?>"><?= $guardrailBlocks24h ?></div>
                <div class="text-muted small">Guardrail blocks (24h)</div>
            </div>
        </div>
    </div>
</div>

<?php if ($runId) { ?>                                                                       
      <div class="alert alert-success d-flex justify-content-between align-items-center">
          <span>Run #<?= $runId ?> completed successfully.</span>          
          
          <?php if ($reportId) { ?>                                                            
              <a href="/report/<?= $reportId ?>" class="btn btn-sm btn-outline-success">View report</a>                                                                                   
          <?php } else { ?>
              <span class="text-muted small">No report generated (guardrail blocked)</span>    
          <?php } ?>                                                                           
      </div>
<?php } ?>   

<div class="card mb-4">
    <div class="card-body">
        <h5 class="card-title">Trigger Research Run</h5>
        
        <form method="POST" action="/dashboard/trigger">
            <div class="mb-3">
                <label for="query" class="form-label">Search query</label>
                <input type="text" id="query" name="query" class="form-control" placeholder="e.g. AI funding Germany 2025" required>
            </div>
            
            <button type="submit" class="btn btn-primary" id="run-btn">Run</button>
            
            <div id="run-loading" class="mt-3 d-none">                                    
                <div class="spinner-border spinner-border-sm text-primary me-2" role="status"></div>
                <span class="text-muted">Running pipeline, please wait…</span>            
            </div>
        </form>
    </div>
</div>

<script src="/assets/js/jquery.min.js"></script>
<script>
    $('form').on('submit', function (){
        $('#run-btn').prop('disabled', true);
        $('#run-loading').removeClass('d-none');
    });
</script>
